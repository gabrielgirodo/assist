export class languageModel{
    private id: number;
    private description: string;

    constructor(pId: number, pDescription: string)
    {
        this.id = pId;
        this.description = pDescription;
    }
}